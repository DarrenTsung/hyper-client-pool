hyper-client-pool:
  build:
    image: hyper-client-pool
    dockerfile: Dockerfile
  depends_on:
    - httpbin
  volumes:
    - .:/hyper-client-pool
  cached: true
httpbin:
  image: kennethreitz/httpbin:latest
