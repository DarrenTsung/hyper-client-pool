hyper-client-pool:
  build:
    image: hyper-client-pool
    dockerfile: Dockerfile
  volumes:
    - .:/hyper-client-pool
  cached: true
httpbin:
  image: httpbin:latest
database:
  image: postgres:latest
